<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:Library.Core.Converters;assembly=Library.Core"
             x:Class="Library.Core.Views.Books.BooksPage"
             Title="Books">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:BoolToNotBoolConverter x:Key="BoolToNotBoolConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="outline_add_white_24.png" Text="Add Book" Command="{Binding AddBookCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
    <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand">

        <Label Visual="Material" Text="Brak ksiazek" IsVisible="{Binding IsInfoVisible}" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>

        <CollectionView Visual="Material" ItemsSource="{Binding Books}" IsVisible="{Binding IsInfoVisible, Converter={StaticResource BoolToNotBoolConverter}}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0"
                           Text="{Binding Title}"
                           FontAttributes="Bold" />
                    <Label Grid.Row="0"
                           Grid.Column="1"
                           Text="{Binding Author}"
                           FontAttributes="Italic"
                           VerticalOptions="End" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    </StackLayout>
    </ContentPage.Content>
</ContentPage>